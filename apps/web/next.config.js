/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Disable standalone mode for development (can re-enable for production if needed)
  // output: 'standalone',
};

module.exports = nextConfig;

